export const metadata = {
  title: 'Chat da Equipe',
  description: 'Chat interno da equipe — acesso pelo computador e pelo celular',
}

export default function ChatLayout({ children }) {
  return children
}
